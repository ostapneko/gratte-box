start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]

respawn
respawn limit 10 5

setuid {{ gratte_user }}
setgid {{ gratte_user }}

exec /home/{{ gratte_user }}/apps/gratte/current/bin/gratte-server
